@implements IComponentModel<ExampleModel>

<CorrelationIdDisplay Model="@(Model.CorrelationId)" />

<TodoExample Model="@(Model)"></TodoExample>

<FileForm Model="@(Model.FileUpload)" />

@code {
    [Parameter] public ExampleModel Model { get; set; } = null!;
}